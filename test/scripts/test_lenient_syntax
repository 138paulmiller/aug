# test single blocks an no semicolons
use std
var a = [ ]
var b = 10

var i = 0 
while i < b
{
    append(a, i % 2);
    i += 1
} 

for e in a 
    expect(e == 0 or e == 1, "e == 0 or e == 1")

func inc(i) 
    return i + 1

var x = 0;
for y in 0,10 {
    expect(x == y)
    x = inc(x)
}
