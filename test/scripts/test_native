use std

func fibonacci(a) {
    if a < 2 { 
        return a; 
    }
    return fibonacci(a-1) + fibonacci(a-2);
}

func count(a) {
    if a <= 0 { 
        return 0; 
    }
    return count(a-1) + 1;
}

# prevent from failing if empty since test is calling local functions
expect( true );