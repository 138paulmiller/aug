func fibonacci(n) {
    var a = 0;
    var b = 1;
    var c = 0;
    var sum = 0;
    var count = 1;
    while count <= n {
        count = count + 1; 
        var d = 9;
        d = 90000;
        print(d);
        a = b;
        b = sum; 
        sum = a + b;
    }
    return sum;
}